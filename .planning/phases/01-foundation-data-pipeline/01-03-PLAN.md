---
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/ingestion/worker.py
  - src/scripts/generate_synthetic_data.py
  - tests/integration/test_ingestion_pipeline.py
  - tests/unit/test_audit_trail.py
autonomous: true
requirements:
  - STREAM-01
  - STREAM-02
  - DB-01
  - DB-02
---

# Plan 01-03: Ingestion Development

Implement the high-throughput Kafka consumer to bridge the ingestion stream and the persistent data store.

## Must Haves
- Batch-based Kafka consumer loop with PostgreSQL `COPY` for bulk loading.
- Functional synthetic data generator for testing and demonstration.
- Clinical audit trail with `received_at` and `processed_at` timestamps correctly populated.

## Tasks

<task id="01-03-00" wave="0">
Initialize `tests/integration/test_ingestion_pipeline.py` and `tests/unit/test_audit_trail.py` with stubs.
</task>

<task id="01-03-01" wave="1">
Implement the ingestion worker logic in `src/ingestion/worker.py` using `confluent-kafka` and `psycopg`.
</task>

<task id="01-03-02" wave="1">
Incorporate the batching and `COPY` protocol into the worker to ensure at-least-once delivery with high throughput.
</task>

<task id="01-03-03" wave="1">
Develop the synthetic data generator script in `src/scripts/generate_synthetic_data.py` to produce realistic vital sign streams.
</task>

<task id="01-03-04" wave="2">
Implement and execute the end-to-end integration test in `tests/integration/test_ingestion_pipeline.py`.
</task>

<task id="01-03-05" wave="2">
Implement unit tests in `tests/unit/test_audit_trail.py` to verify timestamp logic for compliance.
</task>

## Verification Criteria

### Automated Tests
- Run `pytest tests/integration/test_ingestion_pipeline.py` to confirm the full path from Kafka message to DB record.
- Run `pytest tests/unit/test_audit_trail.py` to verify correct timestamp propagation.

### Manual Verification
- Execute `python src/scripts/generate_synthetic_data.py` and monitor `psql` to ensure records are being inserted in real-time.
- Check the worker logs to verify batch sizes and flush intervals match the performance strategy.
